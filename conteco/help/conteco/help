#
# controlplane.conteco
# The conteco controlplane manages the container ecosystem, creating structures of interacting containers.
# The container sets form the structural framework to build functional modules.
#
# The  API consists of repo, config, build, release and deploy, there is no implementation for run.
# The controlplane.conteco controlplane has specific implementations for config (partial) and deploy.
#
