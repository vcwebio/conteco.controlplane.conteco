---
version: '3'
services:

# START SERVICE controlplane.conteco
  controlplane:
    image: ${CONTECO_REGISTRY}${CONTECO_REALM}/${CONTECO_ECOSYSTEM}.${CONTECO_DC_GLOBAL_IMAGE_NAME}
    environment: [${CONTECO_DC_CONTROLPLANE_CONTECO_ENVIRONMENT}] 
    command: [${CONTECO_DC_CONTROLPLANE_CONTECO_COMMAND}]
    volumes: [${CONTECO_DC_CONTROLPLANE_CONTECO_VOLUMES}]
    ports: [${CONTECO_DC_CONTROLPLANE_CONTECO_PORTS}]
    networks: [${CONTECO_DC_CONTROLPLANE_CONTECO_NETWORKS}]
    deploy:
      mode: ${CONTECO_DC_CONTROLPLANE_CONTECO_DEPLOY_MODE}
      resources: 
        limits: 
          cpus: '${CONTECO_DC_CONTROLPLANE_CONTECO_DEPLOY_RESOURCES_LIMITS_CPUS}'
          memory: '${CONTECO_DC_CONTROLPLANE_CONTECO_DEPLOY_RESOURCES_LIMITS_MEMORY}'
        reservations:
          cpus: '${CONTECO_DC_CONTROLPLANE_CONTECO_DEPLOY_RESOURCES_RESERVATIONS_CPUS}'
          memory: '${CONTECO_DC_CONTROLPLANE_CONTECO_DEPLOY_RESOURCES_RESERVATIONS_MEMORY}'
# END SERVICE controlplane.conteco

volumes: {${CONTECO_DC_GLOBAL_VOLUMES}}
networks: {${CONTECO_DC_GLOBAL_NETWORKS}}
